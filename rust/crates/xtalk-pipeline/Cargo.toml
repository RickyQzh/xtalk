[package]
name = "xtalk-pipeline"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Dialogue pipeline orchestration for the X-Talk Rust runtime"

[dependencies]
xtalk-models = { path = "../xtalk-models" }

[dev-dependencies]
tokio = { workspace = true }
