[package]
name = "xtalk-models"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "ASR, TTS, and LLM model interfaces for the X-Talk Rust runtime"

[dependencies]
